# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Secrets / local config
config/secrets.yml
.env
*.local

# Tooling data caches
data/cache/
*.log

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# WorkBuddy project data
.workbuddy/

# Generated artifacts — produced by the eval workflow and published to the
# gh-pages branch. They are NEVER committed to main (keeps main as pure
# source and avoids the recurring "fetch first" push conflict).
data/
dashboard/
